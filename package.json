{
  "name": "glucoseiq",
  "private": true,
  "version": "0.0.0",
  "description": "GlucoseIQ — the headless engine behind glucose on every screen. Zero-dependency TypeScript for CGM & diabetes analytics.",
  "repository": {
    "type": "git",
    "url": "https://github.com/marklearst/glucoseiq.git"
  },
  "homepage": "https://glucoseiq.health",
  "bugs": {
    "url": "https://github.com/marklearst/glucoseiq/issues"
  },
  "author": "Mark Learst",
  "license": "MIT",
  "packageManager": "pnpm@11.12.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "test:packages": "node scripts/package-contract-helpers.test.mjs && turbo run build --filter=@glucoseiq/core --filter=@glucoseiq/react --filter=@glucoseiq/tokens --filter=@glucoseiq/testing --filter=@glucoseiq/cli --filter=diabetic-utils && node scripts/test-package-contracts.mjs",
    "test:launch": "node scripts/test-launch-changeset.mjs && node scripts/release-metadata.test.mjs",
    "clean": "turbo run clean",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "turbo": "^2.5.0"
  },
  "engines": {
    "node": ">=24"
  }
}
